// Basic smoke test for Moku app
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App should have a title', (WidgetTester tester) async {
    // Basic verification that the app code compiles
    expect(true, isTrue);
  });
}
